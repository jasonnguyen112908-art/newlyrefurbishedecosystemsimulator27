// Kedaar wrote out and came up with logic with help of Jason
// this interface lets other classes react when one tile changes
public interface TileChangeListener {
    void tileChanged(int row, int col);
}
