// this enum lists every editing tool the user can choose from the toolbar
public enum ToolType {
    GRASS,
    WATER,
    ROCK,
    PLANT,
    HERBIVORE,
    PREDATOR,
    ERASE_PLANT,
    ERASE_ANIMAL
}
